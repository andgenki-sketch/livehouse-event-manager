'use client';
import {useEffect,useState} from 'react';
import {createClient} from '@supabase/supabase-js';
const supabase=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY);
const label={planning:'企画中',booking:'ブッキング中',confirmed:'確定',promoting:'告知中',show_day:'本番',ended:'終了'};
export default function Page(){
 const [session,setSession]=useState(null),[events,setEvents]=useState([]),[selected,setSelected]=useState(null),[modal,setModal]=useState(false),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[message,setMessage]=useState(''),[loading,setLoading]=useState(true);
 const load=async()=>{const {data,error}=await supabase.from('events').select('*').order('event_date');if(error)setMessage(error.message);else{setEvents(data||[]);setSelected(x=>x||data?.[0]||null)}};
 useEffect(()=>{supabase.auth.getSession().then(({data})=>{setSession(data.session);setLoading(false);if(data.session)load()});const {data:s}=supabase.auth.onAuthStateChange((_e,x)=>{setSession(x);if(x)load();else setEvents([])});return()=>s.subscription.unsubscribe()},[]);
 const auth=async(signup)=>{setMessage('');const {error}=signup?await supabase.auth.signUp({email,password}):await supabase.auth.signInWithPassword({email,password});setMessage(error?error.message:(signup?'登録しました。確認メールが届いた場合は確認してください。':'ログインしました。'))};
 async function add(e){e.preventDefault();const f=new FormData(e.currentTarget);const {data,error}=await supabase.from('events').insert({title:f.get('title'),event_date:f.get('date'),status:f.get('status'),venue:f.get('venue')||null,open_time:f.get('open')||null,start_time:f.get('start')||null}).select().single();if(error)return alert(error.message);setEvents(x=>[...x,data].sort((a,b)=>a.event_date.localeCompare(b.event_date)));setSelected(data);setModal(false)}
 async function del(){if(!selected||!confirm('このイベントを削除しますか？'))return;const {error}=await supabase.from('events').delete().eq('id',selected.id);if(error)return alert(error.message);setEvents(x=>x.filter(e=>e.id!==selected.id));setSelected(null)}
 if(loading)return <div className="auth"><div className="authcard"><h2>読み込み中...</h2></div></div>;
 if(!session)return <div className="auth"><div className="authcard"><small>LIVE HOUSE</small><h1>EVENT MANAGER</h1><p>イベント制作・ブッキングをクラウドで管理</p><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="メールアドレス"/><input value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="パスワード"/><button className="primary" onClick={()=>auth(false)}>ログイン</button><button onClick={()=>auth(true)}>新規登録</button><p>{message}</p></div></div>;
 const days=Array.from({length:35},(_,i)=>i-2);
 return <div className="shell"><aside><h1>LIVE HOUSE</h1><small>EVENT MANAGER</small>{['▣ カレンダー','☷ イベント一覧','◉ アーティスト','☑ タスク','¥ 売上・精算','□ 資料・データ','⚙ 設定'].map((x,i)=><div className={'nav '+(!i?'on':'')} key={x}>{x}</div>)}<button className="signout" onClick={()=>supabase.auth.signOut()}>ログアウト</button></aside><main><header><div><b>‹</b><h2>2026年10月</h2><b>›</b><button>今日</button></div><input placeholder="イベント・アーティストを検索..."/><button className="primary" onClick={()=>setModal(true)}>＋ 新規イベント</button></header><div className="legend">🟢 確定　 🟡 ブッキング中　 🔵 告知中　 🔴 本番　 ⚫ 終了</div><section className="workspace"><div><div className="calendar"><div className="week">{['月','火','水','木','金','土','日'].map(x=><b key={x}>{x}</b>)}</div><div className="grid">{days.map((d,i)=>{const day=d<=0?28+d:d;return <div className="day" key={i}><span>{day}</span>{events.filter(x=>Number(x.event_date.slice(-2))===day).map(ev=><button key={ev.id} className={'event '+label[ev.status]} onClick={()=>setSelected(ev)}>{ev.title}<small>{label[ev.status]}</small></button>)}</div>})}</div></div><div className="lower"><div className="card"><h3>今日やること</h3><p>タスクのクラウド連携を次に実装します。</p></div><div className="card"><h3>今月の状況</h3><p>📅 イベント <b>{events.length}件</b></p><p>✅ 確定 <b>{events.filter(x=>x.status==='confirmed').length}件</b></p></div></div></div><div className="detail">{selected?<><h2>{selected.event_date}<br/>{selected.title}</h2><span className="pill">{label[selected.status]}</span><p>◷ OPEN {selected.open_time?.slice(0,5)||'--:--'} / START {selected.start_time?.slice(0,5)||'--:--'}</p><p>⌖ {selected.venue||'会場未設定'}</p><hr/><h3>出演者・ブッキング</h3><p>アーティストDB連携を次に実装します。</p><button className="danger" onClick={del}>イベントを削除</button></>:<p>イベントを作成してください。</p>}</div></section>{modal&&<div className="modal"><form onSubmit={add}><h2>新規イベント</h2><input name="title" placeholder="イベント名" required/><input name="date" type="date" required/><select name="status"><option value="booking">ブッキング中</option><option value="confirmed">確定</option><option value="promoting">告知中</option><option value="show_day">本番</option><option value="planning">企画中</option></select><input name="venue" placeholder="会場"/><div className="twocol"><input name="open" type="time"/><input name="start" type="time"/></div><div><button type="button" onClick={()=>setModal(false)}>キャンセル</button><button className="primary">クラウドに保存</button></div></form></div>}</main></div>}
